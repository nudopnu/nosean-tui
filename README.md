# Nosean

A TUI for fuzzy-searching your obsidian-vaults.

# Setup

```bash
python -m venv .venv
source .venv/scripts/activate
pip install -r requirements.txt

# run with
python main.py <vault-path>
```
